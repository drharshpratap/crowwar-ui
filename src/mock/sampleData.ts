import { cars } from './cars';
import { users } from './users';
import { flatMessages } from './messages';

export const sampleAuctions = cars;
export const sampleUsers = users;
export const sampleMessages = flatMessages;
